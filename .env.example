# Copy to .env to point the website at the live Hostinger API.
# Leave UNSET (or empty) to keep using the built-in mock data.
# Value = your site origin where /api lives, no trailing slash.
# Example: https://apex.yourdomain.com
VITE_API_BASE=
